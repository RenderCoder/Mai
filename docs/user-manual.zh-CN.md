# Mai 电商文案中文使用手册

这份手册给普通文员、设计师和运营使用。你不需要懂命令行，也不需要记很多参数。

只要记住一个入口：

```text
使用 $mai
```

Mai 默认用简体中文和你沟通。它会先在对话里给你看理解、文案、统计和风险；你确认“可以 / 保存 / 写入”之后，它才会把结果保存成 Markdown 文档。

## 1. Mai 能做什么

- 写 Amazon / AliExpress 标题、副标题。
- 写完整 Listing、五点描述、A+ 模块、标语。
- 根据图片、草图、Figma 截图、详情页版位写图文案。
- 把中文卖点转成自然的英文、德语、西班牙语等文案。
- 检查字数、词数、行数、长单词和二维版式风险。
- 检查合规风险，避免夸大、绝对化、没有依据的宣传。
- 创建产品资料文件夹模板和文案需求模板。
- 检查当前 Mai 版本，并提示如何更新。

## 2. 安装

在 Codex 里输入：

```text
使用 $skill-installer 安装 https://github.com/RenderCoder/Mai/tree/main/skills/mai
```

安装后重启 Codex，然后输入：

```text
使用 $mai
```

只安装 `$mai` 就够了。不要再安装其它复杂入口。

## 3. 检查版本和更新

在 Codex 里输入：

```text
使用 $mai 检查版本
```

Mai 会优先运行：

```bash
sh ~/.codex/skills/mai/scripts/check-version.sh
```

如果显示当前不是最新版，先退出 Codex，再在系统终端运行：

```bash
sh ~/.codex/skills/mai/scripts/update-installed.sh
```

更新后重启 Codex，再检查一次版本。

这个流程不需要安装 Bun、Python、npm 或其它开发环境。

## 4. 卸载

如果你要删除已经安装的 `$mai`，先退出 Codex，再在系统终端运行：

```bash
sh -c 'rm -rf "${CODEX_HOME:-$HOME/.codex}/skills/mai"'
```

这个命令只删除用户级安装的 `$mai` 技能目录。删除后重启 Codex，`$mai` 就不会再被加载。

## 5. 最短上手流程

### 第一步：准备产品资料根目录

建议先准备一个专门放产品资料的总文件夹，例如：

```text
~/my-products/
```

这个文件夹是“产品资料根目录”。每个产品都放在它下面的一个子目录里：

```text
~/my-products/
├── WT801/
├── WT802/
└── WT903/
```

重要：不要把很多产品的资料混在同一个文件夹里。一个产品一个子目录，Mai 才能准确读取上下文。

### 第二步：在产品资料根目录打开 Codex

新手推荐在产品资料根目录打开 Codex，也就是 `~/my-products/`。

这样做的好处是：

- 你能清楚看到有哪些产品子目录。
- 每次生成文案时，可以明确指定产品资料目录。
- 不容易把多个产品资料混在一起。

不推荐的方式：

```text
进入 ~/my-products/WT801/ 之后，只说“使用 $mai 生成标题”
```

原因是：用户可能以为 Mai 会自动知道当前目录就是产品资料目录，但不同工具和运行环境对“当前目录”的理解可能不同。为了避免误读，建议每次都明确写出产品目录。

推荐的方式：

```text
使用 $mai --product ~/my-products/WT801/ 生成标题
```

或者：

```text
使用 $mai 生成标题，产品资料在 ~/my-products/WT801/。
```

### 第三步：创建产品资料文件夹

在 Codex 里输入：

```text
使用 $mai 创建 WT801 产品资料到 ~/my-products/
```

Mai 会创建类似这样的文件夹：

```text
~/my-products/WT801/
└── product.md
```

打开 `product.md`，填入品牌、型号、规格、卖点、关键词等信息。

### 第四步：把更多资料放进同一个产品子目录

你可以继续把资料放进 `~/my-products/WT801/`：

```text
~/my-products/WT801/
├── product.md
├── specs.md
├── faq.md
├── seo-keywords.csv
├── competitors.md
├── image-brief.md
└── old-listing.md
```

Mai 会递归读取这个文件夹里的文本资料。适合放进去的文件包括：

- `.md`
- `.txt`
- `.csv`
- `.json`
- `.yaml`
- `.yml`

不建议把图片、视频、压缩包或无关文档放进产品资料文件夹。

### 第五步：生成文案

生成文案时，建议始终带上具体产品目录：

生成标题：

```text
使用 $mai --product ~/my-products/WT801/ 生成 5 组 Amazon 标题。
```

生成完整 Listing：

```text
使用 $mai --product ~/my-products/WT801/ 生成完整 Listing，语言要中文、英文、德语、西语。
```

根据图片或草图生成副图文案：

```text
使用 $mai --product ~/my-products/WT801/ 根据这张草图写 Amazon 副图文案。
英文主标题不超过 4 个词，副标题不超过 6 个词，标签最多 3 个。
长度用极简表达。
```

如果你只说“使用 $mai 生成标题”，Mai 应该先问你要使用哪个产品目录，而不是直接猜当前目录。

### 第六步：确认后保存

Mai 会先展示结果。你确认后输入：

```text
可以，保存
```

Mai 才会写入 Markdown 结果文档。

## 6. 不知道怎么说时

你可以直接自然语言描述：

```text
使用 $mai 帮我给 WT801 写一套 Amazon 英文五点。
产品资料在 ~/my-products/WT801/。
语气要专业、简洁，避免夸张。
```

Mai 会自己判断任务类型。如果缺少关键信息，它会一次只问一个问题：

```text
我需要先确认一个选项：你要生成哪类文案？
1. 标题/副标题
2. 五点描述
3. 完整 Listing
4. A+ 内容
5. 图片/草图文案
6. 标语/广告语
请回复数字即可。
```

你可以只回复：

```text
2
```

## 7. Mai 会输出什么

保存之前，Mai 应该先给你看：

- 理解确认。
- 当前假设。
- 第一步：初步版本。
- 第二步：第一次反思自查与优化建议。
- 第三步：修正版。
- 第四步：第二次反思与最终调整建议。
- 第五步：最终版本。
- 外语回译核对。
- 数量统计。
- 合规与风险检查。
- 待确认事项。

对话里会尽量避免 Markdown 表格，因为表格原始语法在命令行里不容易读。你确认后，保存下来的 Markdown 文档必须使用表格呈现最终文案、外语回译核对和数量统计，方便复制、对照和交给设计同事。

## 8. 中文优先

Mai 面向中文团队。无论你要求什么语言，最终文案都会先输出简体中文审核版，再输出其它语言。

例如你说：

```text
使用 $mai --product ~/my-products/WT801/ 生成英文和德语标题。
```

实际输出顺序是：

```text
简体中文 -> 英语 -> 德语
```

这样同事可以先用母语检查卖点和表达是否正确。

## 9. 外语回译核对

只看到英文、德语或西语终稿，不代表中文团队已经能确认它真的表达了原本意思。Mai 需要把每个外语终稿再翻译回简体中文，方便你二次核对。

例如最终文案里有德语：

```text
Raumklima per App prüfen
```

Mai 应该补充：

```text
德语回译中文：通过 App 查看/检查室内环境
核对结论：语义基本一致，但“温湿度”被概括成“室内环境”；如果要更明确，可改写。
```

保存成 Markdown 文件时，回译核对要用表格，方便逐行对照。

## 10. 长度怎么选

可以直接用中文说：

- 极简表达：主图、标签、按钮、小版位。
- 中等：副图、详情页模块、常规标题、五点。
- 完整：Listing、A+、SEO 覆盖更完整的内容。

示例：

```text
使用 $mai --product ~/my-products/WT801/ 写图片文案，长度用极简表达。
```

## 11. 常用任务示例

### 标题

```text
使用 $mai --product ~/my-products/WT801/ 生成 5 组标题，长度用中等。
平台是 Amazon。
标题要自然，不要关键词堆砌。
```

### 五点

```text
使用 $mai --product ~/my-products/WT801/ 生成五点描述。
语言要中文、英文、德语、西语。
重点卖点顺序：精准浇水、双区控制、无需 Hub、太阳能供电、耐用接口。
```

### 完整 Listing

```text
使用 $mai --product ~/my-products/WT801/ 生成完整 Listing。
长度用完整。
语气专业、清晰，不要夸张。
```

### A+ 文案

```text
使用 $mai --product ~/my-products/WT801/ 生成 Amazon A+ 文案。
需要 1 个横幅模块、4 个特性模块、1 个对比模块。
语言要中文、英文、德语、西语。
```

### 图片文案

```text
使用 $mai --product ~/my-products/WT801/ 根据这张草图写 Amazon 副图文案。
画面：产品连接两条水管，左边浇花坛，右边浇草坪。
目标用户：家庭园艺用户。
英文主标题不超过 4 个词，副标题不超过 6 个词，标签最多 3 个。
先给我看文案和字符统计，不要直接写文档。
```

### 创建需求文件

```text
使用 $mai 创建图片文案需求文件 wt801-image2 到 ~/tasks/。
```

填好需求文件后：

```text
使用 $mai --product ~/my-products/WT801/ 根据 ~/tasks/wt801-image2.md 生成图片文案。
```

## 12. 长单词和排版

很多语言会比中文更长。德语尤其容易出现很长的复合词。Mai 在生成图片、包装、A+、详情页模块、按钮、标签等文案时，必须主动检查：

- 字符数。
- 词数。
- 行数建议。
- 最长单词或最长连续字符段。
- 是否可能撑破二维版式。

如果德语或其它语言太长，Mai 应该优先改成短词、短语或可自然换行的表达。

## 13. 保存规则

Mai 不会一开始就写文件。它会等你确认。

保存路径优先级：

1. 如果你明确说“保存到某个文件”，就保存到你指定的文件。
2. 如果使用了需求文件，保存到需求文件同目录。
3. 如果只使用了产品资料文件夹，保存到产品文件夹内。
4. 如果无法判断保存位置，Mai 会问你保存到哪里。

保存后的 Markdown 文档应包含：

- 产品资料源路径。
- 已读取的产品资料文件清单。
- 原始需求摘要。
- 场景理解。
- 关键决策。
- 双反思过程。
- 最终文案表：必须是 Markdown 表格，中文列在第一列语言列。
- 外语回译核对表：必须是 Markdown 表格，逐条列出外语原文和回译中文。
- 数量统计表：必须是 Markdown 表格，包含字符数、词数、最长单词/连续字符段、行数建议和是否超限。
- 合规与风险检查。
- 需要人工复核的事项。

## 14. 常见问题

### 我必须写参数吗？

不必须。你可以直接自然语言描述。

### 产品资料必须只有一个文件吗？

不需要。推荐一个产品一个文件夹，里面可以放多个文件。Mai 会递归读取。

### 上传图片后还需要写描述吗？

建议写。图片和草图的文案质量很依赖版位信息。你可以补充：

```text
这是 Amazon 第二张副图，横版。左边是产品，右边放标题和 3 个标签。英文主标题不超过 4 个词。
```

### 合规检查能完全替代人工审核吗？

不能。Mai 会做风险提示，但最终上线前仍建议人工复核，尤其是认证、测试数据、医疗/健康、安全、极限词、平台政策相关内容。

## 15. 给团队用户的最短说明

可以直接把下面这段发给同事：

```text
安装：使用 $skill-installer 安装 https://github.com/RenderCoder/Mai/tree/main/skills/mai

使用：只记住 $mai。

把所有产品放进同一个产品资料根目录，每个产品一个子目录。运行时明确说：
使用 $mai --product ~/my-products/产品型号/ 帮我写标题、五点、Listing、A+ 或图片文案。

Mai 会先用中文说明理解、输出多语言文案和统计，确认后才保存结果文件。
保存后的 Markdown 文件会用表格呈现最终文案、外语回译核对和数量统计。

检查版本：使用 $mai 检查版本。
如果不是最新版，退出 Codex 后运行：
sh ~/.codex/skills/mai/scripts/update-installed.sh

卸载：退出 Codex 后运行：
sh -c 'rm -rf "${CODEX_HOME:-$HOME/.codex}/skills/mai"'
```
